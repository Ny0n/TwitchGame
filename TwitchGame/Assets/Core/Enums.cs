public static class Enums
{
    public enum GameState
    {
        Paused,
        WaitingForPlayers,
        Starting,
        Playing,
        Ending,
    }

    public enum PlayerEventAction
    {
        Add,
        Remove,
        Dead,
    }
}
